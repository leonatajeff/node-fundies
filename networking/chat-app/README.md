Intra Process Communication (OS concept but still applicable to networking)
- if we do `server.list(path...)` it is for IPC servers (replace place with port for TCP servers)
We'll be using the net module for reference: https://nodejs.org/api/net.html


